//https://leetcode.com/problems/reverse-string
class Solution {
public:
    string reverseString(string s) {
        string res="";;
        int n = s.length();
        for (int i = 0; i<n; i++){
            res += s[n-1-i];
        }
        return res;
    }
};
